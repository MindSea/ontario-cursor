/**
 * Secondary copy and trailing action strips on checklist rows when the row reads as done.
 * Shared across workspace checklist sections (not huddle).
 */
export const CHECKLIST_META_WHEN_ROW_DONE = "opacity-70";
